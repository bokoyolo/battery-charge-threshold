# battery-charge-threshold

```
sudo git clone https://github.com/bokoyolo/battery-charge-threshold.git && cd battery-charge-threshold && sudo mv battery-charge-threshold.service /etc/systemd/system/battery-charge-threshold.service && sudo systemctl enable --now battery-charge-threshold.service
```
